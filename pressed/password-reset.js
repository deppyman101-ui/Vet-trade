(()=>{
const SUPA='https://iymhyxheggqsewpczgjl.supabase.co',KEY='sb_publishable_Q8yb_Mw70jDkwld8YGQRRw_h-T5Rau2',AUTH='pressed_auth_v2';
const $=s=>document.querySelector(s);
async function requestReset(email){const redirect=location.origin+'/pressed/account?recovery=1';const r=await fetch(SUPA+'/auth/v1/recover',{method:'POST',headers:{apikey:KEY,'Content-Type':'application/json'},body:JSON.stringify({email,redirect_to:redirect})});let d={};try{d=await r.json()}catch(e){}if(!r.ok)throw new Error(d?.msg||d?.message||d?.error_description||d?.error||'Could not send reset email.');return d}
function show(id){['#accountLoginPanel','#forgotPasswordPanel','#resetPasswordPanel','#accountDashboard'].forEach(s=>{const e=$(s);if(e)e.style.display=s===id?'block':'none'})}
function recoverySession(){const p=new URLSearchParams(location.hash.replace(/^#/,''));if(p.get('type')!=='recovery'||!p.get('access_token'))return null;const s={access_token:p.get('access_token'),refresh_token:p.get('refresh_token')||'',user:null};localStorage.setItem(AUTH,JSON.stringify(s));return s}
async function updatePassword(token,password){const r=await fetch(SUPA+'/auth/v1/user',{method:'PUT',headers:{apikey:KEY,Authorization:'Bearer '+token,'Content-Type':'application/json'},body:JSON.stringify({password})});let d={};try{d=await r.json()}catch(e){}if(!r.ok)throw new Error(d?.msg||d?.message||d?.error_description||d?.error||'Could not update password.');return d}
document.addEventListener('DOMContentLoaded',()=>{const session=recoverySession();if(session){show('#resetPasswordPanel');history.replaceState(null,'',location.pathname+'?recovery=1')}$('#forgotPasswordBtn')?.addEventListener('click',()=>{const e=$('#forgotEmail');if(e)e.value=$('#loginEmail')?.value||'';show('#forgotPasswordPanel')});$('#backToLogin')?.addEventListener('click',()=>show('#accountLoginPanel'));$('#forgotPasswordForm')?.addEventListener('submit',async e=>{e.preventDefault();const msg=$('#forgotMsg'),btn=e.currentTarget.querySelector('button[type=submit]');btn.disabled=true;msg.textContent='Sending reset link…';try{await requestReset($('#forgotEmail').value.trim().toLowerCase());msg.textContent='If that email has a My Presse’d account, a password reset link has been sent. Check your inbox and junk folder.'}catch(err){msg.textContent=err.message}finally{btn.disabled=false}});$('#resetPasswordForm')?.addEventListener('submit',async e=>{e.preventDefault();const pass=$('#newPassword').value,confirm=$('#confirmPassword').value,msg=$('#resetMsg'),btn=e.currentTarget.querySelector('button[type=submit]');if(pass.length<8){msg.textContent='Use at least 8 characters.';return}if(pass!==confirm){msg.textContent='The passwords do not match.';return}let s;try{s=JSON.parse(localStorage.getItem(AUTH)||'null')}catch(x){}if(!s?.access_token){msg.textContent='This reset link is no longer valid. Request a new one.';return}btn.disabled=true;msg.textContent='Updating password…';try{await updatePassword(s.access_token,pass);localStorage.removeItem(AUTH);history.replaceState(null,'','/pressed/account');msg.textContent='Password updated. You can now sign in with your new password.';setTimeout(()=>show('#accountLoginPanel'),1200)}catch(err){msg.textContent=err.message}finally{btn.disabled=false}})});
})();